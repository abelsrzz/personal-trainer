# Planned Workouts

Entrenamientos estructurados listos para exportar o sincronizar.

Los workouts en YAML se pueden subir a Garmin con:

```bash
source .venv/bin/activate
python scripts/garmin/sync_garmin.py schedule-workout-file training/planned/workouts/<archivo>.yaml
```
