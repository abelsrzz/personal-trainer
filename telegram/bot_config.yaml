telegram:
  bot_token: 7962588953:AAH19hZmrostRBmAz6-8OWIzg9dUwklIX8U
  chat_id: 6174679483
  caption_prefix: Running Coach

opencode_remote:
  timeout_s: 3600
  local_retry_timeout_s: 900
  dangerously_skip_permissions: true
  model: openai/gpt-5.4
  # `minimal` mantiene respuestas de Telegram mucho mas cortas y evita carreras
  # de varios minutos para peticiones operativas simples.
  variant: minimal
  gemini_fallback:
    enabled: true
    # api_key: set via GEMINI_API_KEY environment variable (never commit the key)
    models:
      - gemini-2.5-pro
      - gemini-2.5-flash
      - gemini-2.0-flash
